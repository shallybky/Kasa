import logo from '../assets/logo.svg';
import '../styles/tag.css';

function TAG({ tag }) {
    return (
        	<div className="tag">
        		<p> {tag} </p>
        	</div>
        
    )
}

export default TAG